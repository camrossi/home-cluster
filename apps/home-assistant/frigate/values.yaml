config: |
  mqtt:
    host: "zigbee2mqtt"
    port: 8080
  cameras:
    # Define at least one camera
    back:
      ffmpeg:
        inputs:
          - path: rtsp://viewer@10.0.10.10:554/cam/realmonitor?channel=1&subtype=2
            roles:
              - detect