apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: zigbee2mqtt
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`zigbee2mqtt.sabby.local`)
      priority: 10
      services:
        - name: zigbee2mqtt
          port: 8080
          scheme: http