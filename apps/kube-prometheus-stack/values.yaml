defaultRules:
  rules:
    kubeProxy: false
    windows: false
grafana:
  ingress:
    enabled: true
  host:
    name: grafana.sabby.local
    path: "/"
  ingressClassName: "traefik"
kubeProxy:
  enabled: false
prometheus:
  prometheusSpec:
    storageSpec:
      volumeClaimTemplate:
        spec:
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 10Gi
kubeApiServer:
  tlsConfig:
    insecureSkipVerify: true